BFS
   q[0] = s; color[s] = SZURKE; dist[s] = 0;
    e = v = 0;
    int paros = 1;


    printf("BFS sorrend: ");
    while( e <= v ){
        int x = q[e];
        printf("%i ", x);
        for( j = 1 ; j <= n ; ++j ){
            if( adjM[x][j] == 1 && color[j] == FEHER ){
                ++v; q[v] = j;
                dist[j] = dist[x] + 1;
                color[j] = SZURKE;
            }
        }
        ++e;
        color[x] = FEKETE;
    }